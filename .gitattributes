# Handle text files automatically to avoid line ending issues
* text=auto

# Ensure Python files always use LF line endings
*.py text eol=lf

# Ensure commit messages use LF
commitmsg_*.txt text eol=lf

# Binary files - keep untouched
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
